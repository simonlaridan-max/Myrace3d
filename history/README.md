# MyRace3D Publishing History

- `publication_history.json` is the full audit trail with nested parameter snapshots.
- `publication_history.csv` is the Excel-friendly index for filtering and sorting.
